spring:
  application:
    name: deepexi-spring-cloud-kiko
  profiles:
    active: local
swagger:
  base-package: kiko-wangwangwang.controller
  info:
    title: deepexi-spring-cloud-kiko
server:
  error:
    include-stacktrace: always
    include-exception: true
logging:
  file: 'logs/${spring.application.name}.log'
